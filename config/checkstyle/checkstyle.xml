<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
  Checkstyle rule set for springboot-eks-api.

  Philosophy: a gate nobody can keep green is worse than no gate.
  These rules catch real problems (missing Javadoc on public API,
  obviously wrong imports, magic numbers) without blocking idiomatic
  Java 21 code. Adjust per team convention; document every relaxation.
-->
<module name="Checker">
  <property name="severity" value="error"/>
  <property name="fileExtensions" value="java"/>

  <!-- Suppressions file — test classes get relaxed rules -->
  <module name="SuppressionFilter">
    <property name="file" value="${config_loc}/suppressions.xml"/>
    <property name="optional" value="false"/>
  </module>

  <!-- No tab characters -->
  <module name="FileTabCharacter">
    <property name="eachLine" value="true"/>
  </module>

  <!-- Files end with a newline -->
  <module name="NewlineAtEndOfFile"/>

  <module name="TreeWalker">

    <!-- ── Imports ── -->
    <module name="AvoidStarImport"/>
    <module name="UnusedImports"/>
    <module name="IllegalImport"/>
    <module name="RedundantImport"/>

    <!-- ── Naming ── -->
    <module name="TypeName"/>
    <module name="MethodName"/>
    <module name="ParameterName"/>
    <module name="LocalVariableName"/>
    <module name="MemberName"/>
    <module name="StaticVariableName"/>
    <module name="ConstantName"/>
    <module name="PackageName">
      <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
    </module>

    <!-- ── Javadoc on public types and methods (not on trivial getters) ── -->
    <module name="JavadocType">
      <property name="scope" value="public"/>
    </module>
    <module name="JavadocMethod">
      <property name="accessModifiers" value="public"/>
      <property name="allowMissingParamTags" value="true"/>
      <property name="allowMissingReturnTag" value="true"/>
    </module>

    <!-- ── Whitespace ── -->
    <module name="WhitespaceAround"/>
    <module name="NoWhitespaceAfter"/>
    <module name="NoWhitespaceBefore"/>
    <module name="OperatorWrap"/>
    <module name="EmptyLineSeparator">
      <property name="allowNoEmptyLineBetweenFields" value="true"/>
    </module>

    <!-- ── Braces ── -->
    <module name="NeedBraces"/>
    <module name="LeftCurly"/>
    <module name="RightCurly"/>

    <!-- ── Code style ── -->
    <module name="EqualsHashCode"/>
    <module name="SimplifyBooleanExpression"/>
    <module name="SimplifyBooleanReturn"/>
    <module name="StringLiteralEquality"/>
    <module name="FinalLocalVariable">
      <property name="tokens" value="VARIABLE_DEF"/>
    </module>

    <!-- ── Complexity (kept generous to avoid blocking new code) ── -->
    <module name="CyclomaticComplexity">
      <property name="max" value="15"/>
    </module>
    <module name="MethodLength">
      <property name="max" value="80"/>
    </module>
    <module name="ParameterNumber">
      <property name="max" value="8"/>
    </module>

    <!-- ── Misc ── -->
    <module name="UpperEll"/>
    <module name="ModifierOrder"/>
    <module name="EmptyBlock"/>

  </module>
</module>
